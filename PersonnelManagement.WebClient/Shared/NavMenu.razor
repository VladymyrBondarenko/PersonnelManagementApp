<MudPaper Width="250px" Class="d-inline-flex py-3" Elevation="0">
    <MudNavMenu Class="mud-width-full">
        <MudDivider Class="my-2" />
        <MudNavGroup Title="Organization Structure" Icon="@Icons.Filled.MapsHomeWork">
            <MudNavLink Href="departments" Icon="@Icons.Filled.BusinessCenter">Departments</MudNavLink>
        </MudNavGroup>
        <MudNavGroup Title="Personnel management" Icon="@Icons.Filled.ManageAccounts">
            <MudNavLink Href="orders" Icon="@Icons.Filled.FileOpen">Orders</MudNavLink>
        </MudNavGroup>
    </MudNavMenu>
</MudPaper>

@code {
    //private bool collapseNavMenu = true;

    //private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    //private void ToggleNavMenu()
    //{
    //    collapseNavMenu = !collapseNavMenu;
    //}
}
